package spotify

// Authentication constants
const (
	// AuthURL is the URL to Spotify Accounts Service's OAuth2 endpoint.
	AuthURL = "https://accounts.spotify.com/authorize"
	// TokenURL is the URL to the Spotify Accounts Service's OAuth2
	// token endpoint.
	TokenURL = "https://accounts.spotify.com/api/token"
	// BaseURL is the base url to the Spotify web API
	BaseURL = "https://api.spotify.com/v1"
	// ChartBaseURL is the base url to the Spotify charts
	ChartBaseURL = "https://spotifycharts.com"
	// SpotifyBaseURL is the base url to the Spotify web client
	SpotifyBaseURL = "https://open.spotify.com"
)
